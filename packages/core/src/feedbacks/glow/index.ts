/**
 * `@prismui/core/feedbacks/glow` barrel.
 *
 * Re-exports the `glowFeedback` factory. Side-effect CSS import lives in
 * `./glow-feedback.ts` itself (mirrors the ripple pattern).
 */

export { glowFeedback } from './glow-feedback';
